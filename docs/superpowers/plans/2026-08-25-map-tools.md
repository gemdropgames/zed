# Map editor tools — plan

Spec: `docs/superpowers/specs/2026-08-25-map-tools-design.md`.
Branches: zed `map-tools` (off `ggo`), ggo `map-tools` (off `main`).

## Phase A — worldlib (`../ggo/tools/ggo-worldlib`)
What: `MapOp::Fill`, `MapOp::SetCells`, stroke coalescing; `sprites/terrain.rs`.
Why: one undo entry per user action; terrain logic pure and testable.
1. map_doc.rs ops + stroke. Tests. 2. terrain.rs. Tests. 3. Commit, merge main, push.

## Phase B — shared sidecar + cols
4. `ggo_common::tileset_meta` (moved from tileset_panel, + terrains); tileset panel uses it.
5. Map panel reads cols/terrains in worktree frame. Tests. Commit.

## Phase C — fill, select, copy/paste, stroke
6. Tools + actions + keys; strokes around brush/eraser drags; Fill tool.
7. Select tool, overlay, clipboard, paste placement, delete. Tests. Commit.

## Phase D — terrain
8. Terrain tool painting via resolve inside strokes.
9. Terrain editor section + sidecar persistence. Tests. Commit.

## Phase E — wrap
10. clippy + sweep; MIGRATION rows (cols story now accurate); tick task 5; review; merge → ggo, push main.
