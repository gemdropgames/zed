# GGO fork — upstream tracking runbook

Remotes: origin = gemdropgames/zed (push via HTTPS), upstream = zed-industries/zed.
Branches: `main` = pristine upstream mirror (never commit); `ggo` = ours (default).

## Update procedure
    git checkout main && git pull upstream main && git push origin main
    git checkout ggo && git merge upstream/main --no-edit
    cargo test -p ggo_common -p ggo_world_panel -p ggo_sprite_panel \
              -p ggo_charts_panel -p ggo_emu_panel -p ggo_language \
              -p ggo_tileset_panel -p ggo_map_panel -p ggo_import_panel \
              -p ggo_emerald_panel \
              && cargo check -p zed   # all ten ggo crates
Then fix any ggo_* compile breaks (gpui churn) immediately — drift compounds.

## Conflict policy
- `Cargo.lock`: take upstream's (`git checkout --theirs Cargo.lock`), then `cargo check` to re-add ggo deps.
- Registration files (all GGO lines carry `# GGO` / `// GGO` markers):
  - root `Cargo.toml`: `"crates/ggo/*"` member + ggo `[workspace.dependencies]`
  - `crates/zed/Cargo.toml`: ggo deps
  - `crates/zed/src/main.rs`: `ggo_*::init(cx)` calls
  Resolution = keep upstream's version of the region, re-insert the marker lines.
- Behavioural hooks in high-churn upstream files — NOT registration lines, so
  "keep upstream's region and re-insert the markers" does NOT apply; re-apply
  these by hand and re-run the guard tests:
  - `crates/workspace/src/dock.rs`: `Panel::prepare_to_close` (defaulted, so
    upstream panels are unaffected), its `PanelHandle` forwarder, and
    `Dock::panels()` — 4 `GGO` markers, ~24 lines.
  - `crates/workspace/src/workspace.rs`: the panel poll inside
    `Workspace::prepare_to_close` — 2 `GGO` markers, 21 lines. Ordering is
    load-bearing: it must sit AFTER the active-call prompt and BEFORE
    `save_all_internal`.
  Verify with `cargo test -p ggo_world_panel -p ggo_sprite_panel close_guard`
  (8 tests) plus `test_dirty_panel_vetoes_workspace_prepare_to_close`.
  - `crates/workspace/src/workspace.rs`: the `PathOpenInterceptor` registry
    (`register_path_open_interceptor` + `Workspace::intercept_path_open`) — one
    GGO-marked block, ~46 lines, placed right after `register_project_item`
    (F4). Opening AND closing `// GGO` markers (X4 added the closing one), so
    `grep -n GGO crates/workspace/src/workspace.rs` shows the block's extent
    rather than just where it starts. `try_global`-gated: with an empty
    registry every call site behaves exactly as it did before.
  - `crates/project_panel/src/project_panel.rs`: two guarded call sites (F4) —
    the `Event::OpenedEntry` funnel (`open_path_preview`, currently `:893-924`)
    and the `Event::SplitEntry` path (`split_path_preview`, currently
    `:945-964`) — each wraps the existing call in
    `if !workspace.intercept_path_open(&ggo_project_path, window, cx) { … }`.
    **NEW territory for our hooks** as of F4 (previously only `dock.rs` +
    `workspace.rs`'s `prepare_to_close` poll above), and `project_panel.rs`
    churns considerably more than `dock.rs` upstream — expect to hand-reapply
    this one more often. The body is not re-indented; the only edit inside it
    is the `ProjectPath` literal hoisted to a `ggo_project_path` local (needed
    twice — once by the interceptor, once by the open). That hoist is the
    8 deleted lines in the F4 diff; everything else inside the guard is the
    original statement, unmoved (`cargo fmt --check` clean before and after).
    Verify with `cargo test -p workspace -p project_panel --lib` (one `--lib`
    for both packages — cargo rejects the flag twice) plus
    `cargo test -p ggo_world_panel -p ggo_sprite_panel -p ggo_tileset_panel`
    (interceptor-routing + already-open-fast-path tests) and, above all, the
    two tests in the fork that each enter at one of the real
    `project_panel::Event` subscriptions, so a dropped guard fails the
    matching test rather than merging in silently: `cargo test -p
    ggo_emu_panel test_project_panel_opened_entry` covers the
    `Event::OpenedEntry` funnel (X4; verified by deleting the guard and
    watching it go red) and `cargo test -p ggo_emu_panel
    test_project_panel_split_entry` covers the `Event::SplitEntry` path
    (F4 review follow-up; same red/green drill). Both guarded call sites are
    now covered. Every other routing test enters at
    `Workspace::intercept_path_open` and would stay green.
  - `crates/workspace/src/workspace.rs`: the `ContextMenuContributor` registry
    (`register_context_menu_contributor` + `Workspace::context_menu_contributions`)
    — the fork's **third** behavioural hook (F5.0/G1), one GGO-marked block of
    80 lines (`:1014-1092`) with opening AND closing markers, placed after the
    `PathOpenInterceptor` block above. Same `try_global` gate: an absent or
    empty registry returns an empty `Vec` and the menu is byte-identical to
    upstream's. A contributor returns `Vec<ui::ContextMenuItem>` rather than
    taking and returning the half-built `ContextMenu` on purpose — `ContextMenu`
    exposes no item count, so "did anything get added?" would be unanswerable
    and the separator the call site emits ahead of the fork's block would be
    stray whenever every contributor declined. Non-local projects (SSH remote,
    collab guest) contribute nothing, mirroring
    `ggo_common::rel_in_primary_worktree`'s rule. **Contributors run while
    `ProjectPanel` is leased** (`deploy_context_menu` is reached through
    `cx.listener`), so one that reaches back for
    `workspace.panel::<ProjectPanel>(cx)` and reads or updates it panics
    ("cannot read/update ProjectPanel while it is already being updated").
    Entry *handlers* run later, after the lease is released, and are free of
    this. If a contributor needs more predicate context than
    `(path, is_dir)` — `is_read_only`, `is_root`, `marked_entries` — add it as
    a PARAMETER: all three are already locals in scope at the call site, so it
    is a one-line change inside the same GGO chunk, and it keeps contributors
    out of the leased entity.
  - `crates/project_panel/src/project_panel.rs`: a THIRD guarded call site
    (F5.0/G1), and the **second** hook in this file — inside
    `deploy_context_menu` (`:1061`), two GGO-marked chunks at `:1122-1136` and
    `:1138`/`:1251-1259`:
      1. `:1122-1136` — a `ggo_project_path` + `ggo_items` local computed just
         before `ContextMenu::build`. Purely additive.
      2. `:1138` and `:1251-1259` — the builder closure's body becomes a
         statement: upstream's `menu.context(self.focus_handle.clone()).map(…)`
         gains a `let menu = ` prefix, its closing `})` gains a `;`, and the
         closure then ends with the fork's
         `if ggo_items.is_empty() { menu } else { menu.separator().extend(ggo_items) }`.
    29 added lines, **2 deleted** — and the 2 deletions are those same 2 lines
    re-emitted, nothing else. Why not chain a second `.map(…)` onto upstream's
    (which would have been additive-only): rustfmt then re-indents the entire
    ~120-line builder chain, turning every future merge conflict here into the
    whole menu. `cargo fmt --check` is clean before and after the `let` form.
    **Merge recovery**, when this conflicts: take upstream's `deploy_context_menu`
    wholesale, paste chunk 1 back in verbatim after
    `let has_pasteable_content = …;`, then re-apply chunk 2 by hand — add
    `let menu = ` to the `menu.context(…)` line, a `;` to the `})` that closes
    it, and paste the `if/else` before the closure's final `}`. Dropping the
    `let`/`;` half does not compile, so a half-applied merge fails loudly.
    Order is load-bearing: the fork's entries go last, after everything
    upstream builds, and the `separator()` is inside the `else` branch so an
    empty registry adds no divider.
    Verify with `cargo test -p workspace -p project_panel --lib` (237 + 116;
    one `--lib` for both packages) plus, above all,
    `cargo test -p ggo_emu_panel context_menu` (4 tests) —
    `test_project_panel_context_menu_shows_a_contributed_entry` builds a REAL
    `ProjectPanel`, docks it, right-clicks a real row and looks for the
    contributed entry in the RENDERED menu (below every upstream entry), so a
    dropped hook fails it rather than merging in silently (verified red twice:
    with the `if/else` neutered to `menu`, and with both GGO chunks deleted).
- Anything else conflicting means upstream moved a registration site: relocate the marker line, never keep a stale copy of upstream code.
- After each merge: verify `reload_keymaps` (crates/zed/src/zed.rs) still ends with `keymap_editor::KeymapEventChannel::trigger_keymap_changed` — ggo_world_panel's, ggo_sprite_panel's, ggo_charts_panel's, and ggo_emu_panel's keybindings all silently die if that call disappears.

## Rules
- All GGO code lives in `crates/ggo/*`. Upstream files get registration lines only.
- Never commit non-ggo changes on `ggo`; upstream itches become upstream PRs off `main`.
- Cadence: merge at least per upstream stable tag (~weekly).

## Measurements

Entries below are a dated log and are **not** rewritten after the fact. Several
mention `ggo_hello`, the F0 fork-wiring smoke crate: it was deleted on
2026-08-09 (X4) once five real panels proved the same wiring, so any `-p
ggo_hello` in an older line is history, not a command to run today. The current
sweep is TEN crates, and the update procedure above now lists all ten (F5.1
added `ggo_map_panel` and `ggo_import_panel`; F5.2/S3 added the tenth,
`ggo_emerald_panel`).

- 2026-08-07 cold `cargo build -p zed`: 7m 54s
- 2026-08-07 incremental rebuild after touching ggo_hello: 7.58s
- (2026-08-07, ggo_hello since deleted) pane smoke: headless gpui test added (test_open_hello_action); human visual run still welcome
- 2026-08-07 merge drill: upstream/main was zero-delta (0 new commits since clone) — `git merge upstream/main --no-edit` was a no-op, no conflicts. `cargo test -p ggo_hello && cargo check -p zed` passed clean, fully cached (<1s). Procedure validated but not exercised against real upstream churn — repeat drill pending in a few days before F1 starts.
- 2026-08-08 merge drill #2 (real): 6 upstream commits, 0 conflicts, ggo_hello 3/3 + check clean in ~40s
- 2026-08-08 F1 wrap (W5-W7 landed: world panel skeleton/viewer/editor): `cargo test -p ggo_hello -p ggo_world_panel` 3+30 passed, `cargo clippy -p ggo_world_panel --all-targets` clean, `cargo build -p zed` 3m 25s (warm incremental, not cold)
- 2026-08-08 merge drill #3 (F1 wrap, real): 1 upstream commit (`08827f9208` terminal `starts_open` setting), 0 conflicts, `git merge upstream/main --no-edit` clean; `cargo test -p ggo_hello -p ggo_world_panel` 3+30 passed, `cargo clippy -p ggo_world_panel --all-targets` clean, `cargo check -p zed` 35s; `reload_keymaps` invariant re-verified intact post-merge
- 2026-08-08 F2 wrap (M2-M7 landed: metasprite panel viewer/editor/clips/transport/save + world panel add-delete/save-root parity): `cargo test -p ggo_world_panel -p ggo_metasprite_panel` 32+36 passed, `cargo clippy -p ggo_world_panel -p ggo_metasprite_panel --all-targets -- -D warnings` clean, `cargo build -p zed` 2m 45s (warm incremental, not cold)
- 2026-08-08 F3 wrap (C1-C2 charts panel, E1-E2 emu pane, L1 GGO World language, T1 `.zed` task layer, cleanup landed): `cargo test -p ggo_hello -p ggo_common -p ggo_world_panel -p ggo_metasprite_panel -p ggo_charts_panel -p ggo_emu_panel -p ggo_language` = 3+5+37+41+95+76+15 = 272 passed, 0 failed; `cargo clippy` on all seven `--all-targets -- -D warnings` clean; `cargo fmt --check` clean; merge drill #4: `git rev-list --count ggo..upstream/main` = 0 (upstream/main still `08827f9208`, unchanged since drill #3) — no merge to exercise, `reload_keymaps` invariant re-verified intact (`crates/zed/src/zed.rs:2356`). Feature disposition audit vs ggo-ide: `docs/ggo/MIGRATION.md`.
- 2026-08-09 merge drill #5 (F4 wrap, real): `git rev-list --count ggo..upstream/main` = 2 (`371a7d4ba2` editor cmd-click references fallback, `59b2ebf103` gpui `img` aspect-ratio fix — neither touches `workspace.rs`, `dock.rs`, or `project_panel.rs`); `git checkout main && git pull upstream main && git push origin main` fast-forward `08827f9208..371a7d4ba2`; `git checkout ggo && git merge upstream/main --no-edit` clean, 0 conflicts (`ort` strategy, 4 files outside `crates/ggo/*`); `reload_keymaps` invariant re-verified intact (still ends with `keymap_editor::KeymapEventChannel::trigger_keymap_changed`, `crates/zed/src/zed.rs:2356`).
- 2026-08-09 F4 wrap (X1-X3: explorer-driven panel routing, `ggo_tileset_panel` read-only `.til` viewer, docs + sweep), post-merge: `cargo test -p ggo_hello -p ggo_common -p ggo_world_panel -p ggo_metasprite_panel -p ggo_charts_panel -p ggo_emu_panel -p ggo_language -p ggo_tileset_panel` = 3+5+43+46+95+76+15+13 = **296 passed, 0 failed** (all EIGHT ggo crates); `cargo clippy` on all eight `--all-targets -- -D warnings` clean; `cargo fmt --all --check` clean; `script/check-licenses` exit 0; `cargo check -p zed` clean; `cargo test -p workspace --lib` = 237 passed; `cargo test -p project_panel --lib` = 116 passed (both touched by X1's interceptor hooks). Feature disposition re-tally: `docs/ggo/MIGRATION.md` Counts, dropped → partial by one row (`.til` viewer).
- 2026-08-09 F4 X4 (`.cart` explorer routing — the last in-panel picker removed; F4 final-review doc corrections; `ggo_hello` deleted): `cargo test -p ggo_common -p ggo_world_panel -p ggo_metasprite_panel -p ggo_charts_panel -p ggo_emu_panel -p ggo_language -p ggo_tileset_panel` = 5+43+46+95+71+15+13 = **288 passed, 0 failed** (all SEVEN remaining ggo crates; `ggo_emu_panel` 76 → 71: −9 for the deleted enumeration/dropdown tests, +4 for routing); `cargo clippy -p ggo_emu_panel -p ggo_common --all-targets -- -D warnings` clean; `cargo fmt --all --check` clean; `script/check-licenses` exit 0; `cargo check -p zed` clean (the real test of the `ggo_hello` de-registration); `cargo test -p workspace --lib` = 237 passed; `cargo test -p project_panel --lib` = 116 passed. New: `ggo_emu_panel`'s `test_project_panel_opened_entry_routes_a_cart_into_the_panel` enters at the REAL `project_panel` `Event::OpenedEntry` subscription — verified red by deleting the GGO guard, green with it. Feature disposition: no status moved, `MIGRATION.md` Counts unchanged.
- 2026-08-09 F4 X4 review follow-up (closes the review's remaining items): added `ggo_emu_panel::test_project_panel_split_entry_routes_a_cart_into_the_panel`, the `Event::SplitEntry` sibling of the `OpenedEntry` test above — same real-`ProjectPanel` shape, verified red by neutralizing the `:955` `split_path_preview` guard and green again with it restored (`git diff crates/project_panel/` empty after). Both project_panel.rs call sites are now covered. Also fixed three stale comments in `ggo_emu_panel.rs` (module doc, `run_generation` field doc, completion-closure comment) left over from `refresh_carts`/`load_generation`'s deletion in 51f351a9c3, and dated the `UPSTREAM.md` pane-smoke line as `ggo_hello`-era history. `cargo test -p ggo_common -p ggo_world_panel -p ggo_metasprite_panel -p ggo_charts_panel -p ggo_emu_panel -p ggo_language -p ggo_tileset_panel` = 5+43+46+95+72+15+13 = **289 passed, 0 failed**; `cargo clippy -p ggo_emu_panel --all-targets -- -D warnings` clean; `cargo fmt --all --check` clean; `script/check-licenses` exit 0; `cargo check -p zed` clean; `cargo test -p project_panel --lib` = 116 passed.
- 2026-08-09 F5.0 G1 (project-panel context-menu contributor hook — the fork's THIRD behavioural hook and SECOND site in `project_panel.rs`): registry `crates/workspace/src/workspace.rs` +80/−0 (one GGO-marked block, `:1014-1092`, right after the `PathOpenInterceptor` block), call site `crates/project_panel/src/project_panel.rs` +29/−2 (two GGO chunks in `deploy_context_menu`; the 2 deletions are the same 2 lines re-emitted with `let menu = ` / `;` — chaining a second `.map(…)` instead would have been additive-only but made rustfmt re-indent the whole ~120-line builder chain). Contributor signature returns `Vec<ui::ContextMenuItem>` rather than taking/returning the half-built `ContextMenu`: `ContextMenu::items` is private with no length accessor, so "did anything get added?" would be unanswerable and the separator would render stray whenever every contributor declined. `cargo test -p ggo_emu_panel` = **76 passed** (72 baseline + 4: rendered-menu canary incl. below-upstream ordering, registration order, empty registry, non-local decline); `cargo test -p workspace -p project_panel --lib` = 237 + 116 passed; `cargo clippy -p ggo_emu_panel --all-targets -- -D warnings` and `-p workspace -p project_panel --lib` clean; `cargo fmt --all --check` clean; `script/check-licenses` exit 0; `cargo check -p zed` clean. Red drill: canary verified red twice (the `if/else` neutered to `menu`; both GGO chunks deleted — the latter still compiles, which is exactly the silent-merge case) and green again with `git diff crates/project_panel/` back to the intended hook.
- 2026-08-09 F5.0 wrap (G2 + G3: first real context-menu entries — Delete World, Duplicate/Delete Sprite — on the G1 contributor hook, plus `ggo_common::confirm_destructive`), post-merge: `cargo test` on all seven ggo crates = **313 passed, 0 failed**; `cargo clippy` on all seven `--all-targets -- -D warnings` clean; `cargo fmt --all --check` clean; `script/check-licenses` exit 0; `cargo check -p zed` clean; `cargo test -p workspace -p project_panel --lib` = 237 + 116 passed (note the corrected command form — the previously documented `-p workspace --lib -p project_panel --lib` errors with "the argument '--lib' cannot be used multiple times" and had never run as written). Destructive ops confirm via `window.prompt` and name unsaved edits when the target is the open document; failed unlinks now log. `MIGRATION.md` Counts: ✓ 32 / partial 30 / deferred 29 / dropped 12 (Delete world deferred → ✓; Sprite rail ops deferred → partial).
- 2026-08-09 merge drill #6 (F5.0 wrap, real): upstream/main had moved; `git merge upstream/main` merged clean into `ggo` as `b40e8ecd01` (66 upstream files, 0 conflicts, no GGO file touched by the merge). Post-merge verification at the shipped tree: all 49 `GGO` markers present across the six registration/hook files (root `Cargo.toml` 13, `crates/zed/Cargo.toml` 6, `crates/zed/src/main.rs` 6, `dock.rs` 4, `workspace.rs` 11, `project_panel.rs` 9); the context-menu hook's four canary tests in `ggo_emu_panel` all green, which is the check that matters — a merge is exactly when a hand-reapplied `project_panel.rs` hook can vanish silently. `reload_keymaps` still ends with `keymap_editor::KeymapEventChannel::trigger_keymap_changed`, so panel keybindings survive.
- 2026-08-10 F5.1 wrap (Track I: `import.rs` moved into worldlib (ggo PR #81) + `ggo_import_panel`, tileset-only PNG import — crop/grid/quantize/commit to `.til`/`.pal`, "Import as tileset…" on a `.png`; Track M: `compose_map_rgba` shared into worldlib (ggo PR #79), `palette565::{slot_rgba,indices_to_rgba}` collapsing five duplicate indexed→RGBA expansions (ggo PR #80), `ggo_map_panel` — full map authoring: brush/multi-tile stamp/rect-fill/erase/eyedropper/flip/palSub/grid/zoom/resize/undo/save, `.map` interceptor, "New Map…"; wrap carried items: `.map` interceptor doc-comment ordering fix, a `clippy.toml` `disallowed-methods` entry pinning `ggo_worldlib::sprites::import::WizardState::targets` (raw, `assets/`-prefixed-rel-bug-prone) in favor of `OpenImport::dest_targets()`, and a direct `compose_map_rgba` unit test landed via ggo PR #82), across BOTH repos: `cargo test -p ggo-worldlib -p ggo-ide` = 463 + 1386 passed (ggo repo, PR #82 pre-merge); fork sweep post-drill (below) all NINE ggo crates = 95+7+76+33+15+44+57+13+50 = **390 passed, 0 failed** (`ggo_map_panel` and `ggo_import_panel` are new since F5.0's seven); `cargo clippy` on all nine `--all-targets -- -D warnings` clean; `cargo fmt --all --check` clean; `script/check-licenses` exit 0; `cargo check -p zed` clean; `cargo test -p workspace -p project_panel --lib` = 237 + 120 passed (project_panel 116 → 120: upstream's undo-trash-confirm commit in this merge added 4). `MIGRATION.md` Counts: ✓ 32 / partial 32 / deferred 28 / dropped 11 (Map editor dropped → partial on M2's ship, PNG import wizard deferred → partial on I2's ship — both folded into one re-tally here since the M2 fix-round commit updated its row's text without re-tallying the table).
- 2026-08-10 merge drill #7 (F5.1 wrap, real): `git rev-list --count ggo..upstream/main` = 6 (`4bd1993783` markdown triple-tilde mermaid fences, `d4010e91cc` project_panel undo-trash-confirm, `4e8057d74d` ci assignee workflow, `0812d21017` open_ai model recommendation, `492acd6c81` revert of a terminal process-group-close change, `f99aad788b` guild-role labeling); `git checkout main && git pull upstream main && git push origin main` fast-forward `1271f8b0e8..4bd1993783`; `git checkout ggo && git merge upstream/main --no-edit` clean, **0 conflicts** despite `crates/project_panel/src/project_panel.rs` carrying 149 changed lines (the `ort` strategy auto-merged around both GGO hook chunks — `git diff` shows only upstream's own lines touched). Post-merge: all 9 `GGO` markers intact in `project_panel.rs` (both call sites: `OpenedEntry`/`SplitEntry` guards at `:904-975`, the context-menu contributor chunks at `:1133-1270`); registration-file marker counts grew as expected for two new crates since the last drill (root `Cargo.toml` 13→15, `crates/zed/Cargo.toml` 6→8, `crates/zed/src/main.rs` 6→8; `dock.rs` and `workspace.rs` unchanged at 4/11 — neither gets a per-crate line); `reload_keymaps` still ends with `keymap_editor::KeymapEventChannel::trigger_keymap_changed` (`crates/zed/src/zed.rs:2356`). Full nine-crate + workspace/project_panel/licenses/check-zed sweep re-run clean post-merge (see the F5.1 wrap line above, run after this drill).
- 2026-08-10 F5.2 wrap (S1-S5, across both repos): **ggo repo** — PR #83 moved `manifest_ops.rs` into `ggo_worldlib::emerald` UI-free (72 tests travelled, `ggo-worldlib` 463→535 / `ggo-ide` 1386→1314), field-kind validation unified onto `schemas::parse_field_kind`. **fork** — `ggo_metasprite_panel` renamed `ggo_sprite_panel`; tileset-tile picker; New Sprite…/New Metasprite… (bound via a panel form, default skips a tileset another sprite already owns); Rename Sprite… (S2, `6dd981cd3b`+`e79fb0a4fa`). New `ggo_emerald_panel` (**tenth** ggo crate) with an injectable `EmdRunner` and six `emd generate` forms; New Component…/System…/Schedule…/World…/Tileset… menu entries; fixed a live world-panel bug where `manifests/` was resolved relative to the asset root, so an asset-rooted world got builtins-only inspector schemas (S3, `f8b0beae01`). "Emulate this world (cart)", "Re-run (perf)", "Run hardware diagnostics" context-menu entries in `ggo_emu_panel`; `emerald_project_root` + the proc-runner lifted into `ggo_common` (five call-site copies collapsed to one) (S4, `6ce01006be`+`f05457a040`). S5 wrap: two carried doc fixes (`menu.rs`'s stale "anywhere" doc vs. the `is_dir` diagnostics gate; `MIGRATION.md`'s stale "historic overlay" claim — there is no overlay yet, the real risk is picker-list ambiguity), `MIGRATION.md` re-tally (five rows closed: New tileset/map, "+ New world", Emulate, Re-run, hardware diagnostics — before ✓ 35/partial 31/deferred 27/dropped 11 total 104, after ✓ 38/partial 33/deferred 22/dropped 11 total 104). Ten-crate fork sweep: `cargo test` on all ten ggo crates = 95+15+30+98+33+15+44+74+13+54 = **471 passed, 0 failed**; `cargo clippy` on all ten `--all-targets -- -D warnings` clean; `cargo fmt --all --check` clean; `script/check-licenses` exit 0; `cargo check -p zed` clean; `cargo test -p workspace -p project_panel --lib` = 237 + 120 passed.
- 2026-08-10 merge drill #8 (F5.2 wrap, real): `git rev-list --count ggo..upstream/main` = 4 (`9e236090b9` gpui per-window frame-duration/present-interval histograms, `c65e08a83d` syntax-layer panic fix, `069449ab71` docs smart-punctuation, `e4671f71d5` malformed-tasks startup error toast); `git checkout main && git pull upstream main && git push origin main` fast-forward `4bd1993783..9e236090b9` (local `main` was 6 commits ahead of `origin/main` from prior drills' fast-forwards not yet pushed — pushed here too); `git checkout ggo && git merge upstream/main --no-edit` clean, 0 conflicts (`ort` strategy auto-merged `crates/zed/Cargo.toml`, the only GGO-marked file upstream's 12-file diff touched). Post-merge marker counts grew as expected for the tenth crate (`ggo_emerald_panel`, added in S3 but not yet drilled): root `Cargo.toml` 15→16, `crates/zed/Cargo.toml` 8→9, `crates/zed/src/main.rs` 8→9; `dock.rs`/`workspace.rs`/`project_panel.rs` unchanged at 4/11/9. `reload_keymaps` still ends with `keymap_editor::KeymapEventChannel::trigger_keymap_changed` (`crates/zed/src/zed.rs:2401`). Full ten-crate + workspace/project_panel/clippy/fmt/licenses/`cargo check -p zed` sweep re-run clean post-merge (see the F5.2 wrap line above, run after this drill — same 471/237/120 with no deltas from the merge).
- 2026-08-10 F5.3 wrap (E1-E6, across both repos): **ggo repo** — PR #84 moved the `emd` version lock into `ggo_worldlib::emerald` (`EXPECTED_EMD_VERSION`, `compare_lock`/`check_version`, `LockStatus`, `EmdError` and its four mismatch phrasings); PR #85 added a coalescing guard in `world_doc` (a gesture id may only amend the top undo entry when that entry is the same gesture); PR #86 (this wrap's carried fix) made `EmdError`'s `Missing` phrasing HOST-NEUTRAL — it named ggo-ide's Settings page, so every other host had to append a correcting sentence and the fork's banner read as an instruction followed by its own retraction. All four phrasings now name only "your emd path override or PATH"; ggo-ide appends its Settings sentence and the fork appends its `GGO_EMD` one, each only where the remedy is a lookup problem (`Missing`/`Unreachable`, not `CliOld`/`CliNew`). ggo-side test counts: `ggo-worldlib` 547, `ggo-ide` 1305, both green. **fork** — E2 `edd26d80ef` emerald remove ops (component/system/schedule + field add/remove) with cascade-aware confirms (systems exact via `schedules_using_system`; components scanned against `assets/worlds/**`, with the "Rust code that names it is not scanned" limit printed in the prompt itself), a 600 s `EMD_TIMEOUT` with `kill_on_drop`, and a `Reverted` run state distinct from a failure; E3 `3545408a62` the schedule ordered run-list editor (Up/Down, trash, "+ System", per-row cadence picker, optimistic commit with a visible rollback that re-reads the manifest); E4 `f4aa398ab5` the version-lock banner + gating on `start_run` + a 30 s mtime poll + mid-run drift; E5 `8e093d6e3f` + `657370636c` the four carried panel deferrals (onion skin with ggo-ide's directional red/blue tint, world grid/Reset/preview stepper, arrow-key nudge with undo coalescing, MetaSprite goto-sprite). Ten-crate fork sweep: `cargo test` on all ten = 95+18+78+98+33+15+44+92+13+68 = **554 passed, 0 failed** (471 at F5.2, +83); `cargo clippy` on all ten `--all-targets -- -D warnings` clean; `cargo fmt --all --check` clean; `script/check-licenses` exit 0; `cargo check -p zed` clean; `cargo test -p workspace -p project_panel --lib` = 120 + 237 passed. `MIGRATION.md` re-tally (seven rows moved, the largest single-phase movement so far): before ✓ 38 / partial 33 / deferred 22 / dropped 11, after ✓ 44 / partial 34 / deferred 15 / dropped 11, total 104 unchanged. Six new entries in the Known-deferrals ledger, all out of F5.3's own reviews (unreadable-manifest-reads-as-absent and its rollback consequence, the on-UI-thread world scan, the dropped second confirm continuation, `emerald_dir`'s worktree-root fallback, the onion `ghost_cache`'s growth with the strip, and `kill_on_drop` not reaching `emd`'s `cargo` grandchild).
- 2026-08-10 merge drill #9 (F5.3 wrap, real): `git rev-list --count ggo..upstream/main` = 1 (`a1860ac1c1` "Document Claude Opus 5 hosted model pricing", a five-line docs-only change to `docs/src/account/zed-hosted-models.md`); `git checkout main && git pull upstream main && git push origin main` fast-forward `9e236090b9..a1860ac1c1`; `git checkout ggo && git merge upstream/main --no-edit` clean as `5163e8cab0`, 0 conflicts, no GGO-marked file touched. Marker counts unchanged from drill #8 (root `Cargo.toml` 16, `crates/zed/Cargo.toml` 9, `crates/zed/src/main.rs` 9, `dock.rs` 4, `workspace.rs` 11, `project_panel.rs` 9 — no new crate this phase, `ggo_emerald_panel` was already the tenth). `reload_keymaps` still ends with `keymap_editor::KeymapEventChannel::trigger_keymap_changed` (`crates/zed/src/zed.rs:2401`). Full ten-crate + workspace/project_panel/clippy/fmt/licenses/`cargo check -p zed` sweep re-run clean post-merge — same 554/120/237 with no deltas from the merge (as expected for a docs-only commit).
- 2026-08-10 F5.4 wrap (R1-R7, across both repos — the reports-depth block and emulator audio): **ggo repo, 2 PRs** — PR #87 (`4b8e9dff`) extracted report aggregation out of `pages/reports.rs` into `ggo_worldlib::charts::reports::{fmt, perf_db, diag_db, kpi, gates, ignore, inspect, profile, historic, uart_diag}` (the eighth extraction of this kind; `reports.rs` 5347 → 3274 lines, 114 tests travelled + 5 new, 33 `pub(super)` → `pub` widenings, all documented). What stayed behind is stated rather than glossed: every `Element`-returning fn, `backend/db.rs` (its signatures are `iced::Task`-typed), `DiagKind`/`DiagManager`, and `rerun::matches_stored` (reaches `backend::cartlib`). PR #88 (`ec6a4b36`) turned `clone_runs` from copy-once into a **reconcile**: a `RUNS_COLS` fingerprint re-clones a changed source row, children are `DELETE`d before re-insert, and each run is one `BEGIN IMMEDIATE` that writes the `runs` marker **last**. That fixed two permanent data-corruption paths found in R3's review — a run cloned mid-flight was frozen forever as "still running, no verdict, truncated log" with no recovery short of deleting `ggo_ide.db`, and two concurrent callers could double-insert children that `INSERT OR IGNORE` cannot dedupe (no unique key) — and the delete-first shape self-heals databases the old code had already corrupted. ggo-side suites re-run at this wrap: `ggo-worldlib` **668** (547 at F5.3), `ggo-ide` **1193** (1305 at F5.3 — the drop is tests travelling to worldlib, not tests lost), both green. **fork, 6 commits** — R6 `a868a3e6c2` emulator audio: the panel owns a `cpal` output stream but opens it *inside the per-run thread*, after the cart parses and dropped before perf serialisation, so the device's lifetime is the run's (ggo-ide holds one across every idle stretch, which is why F3 deferred this); plus `ctrl-alt-m` mute and a dropout counter that counts *events*, not starved samples. R2 `b86aae8858` KPI tiles, run detail header + config line, failed-asset and panics tables. R3 `37ec582dac` run history rail, stored UART console, charts→emu Re-run through a `ggo_common::CartRunner` registry — with the `diag.db` clone moved off the UI thread behind a tripwire. R4 `c34d537424` click-to-inspect + I$ profile table, and `guarded_listener` (a boxed wrapper around `cx.listener`; the hand-copied gpui internal an earlier round introduced was removed). R5 `63fee1187c` historic overlays, drag-zoom, and the per-chart scene memo. Ten-crate fork sweep: `cargo test` on all ten = 211+18+78+131+33+15+44+92+13+68 = **703 passed, 0 failed** (554 at F5.3, **+149**); `cargo clippy` on all ten `--all-targets -- -D warnings` exit 0; `cargo fmt --all --check` exit 0; `script/check-licenses` exit 0; `cargo check -p zed` exit 0; `cargo test -p workspace -p project_panel --lib` = 237 + 120 passed. `Cargo.lock` verified **committed-clean**: R6's review flagged that worldlib's new `ggo-db`/`turso`/`tokio` deps re-dirty it, but they were already workspace deps pulled directly by `charts_panel` and `emu_panel`, the delta was 3 lines and no new package entered the graph — the refresh is in the tree and a full test+clippy+check sweep leaves `git status` clean. `MIGRATION.md` re-tally (**thirteen rows moved**, the largest single-phase movement and the one that empties the deferred column): before ✓ 44 / partial 34 / deferred 15 / dropped 11, after ✓ 56 / partial 35 / deferred 2 / dropped 11, total 104 unchanged. What is left deferred is **LSP plus two stragglers** — §10's version/environment row + `emd` re-check, and §7's "Copy for agent" export — and `MIGRATION.md` now names that set in full rather than leaving it to be counted off the table. Eight new entries in the Known-deferrals ledger out of F5.4's own reviews (`stop()` not joining so a restart can overlap two audio streams by ~16 ms and a stale verdict write can show run A's status for run B; Windows/COM audio untested and cart-mode-only, with `pump_audio` named as the full-system seam; up to 10 short-lived tokio runtimes per run selection, fixable worldlib-side with a shared connection; the ~11.8 ms memoized hover move at the 100k-frame cap, whose real fix is decimating by index *before* `plot_points` materialises 100k points that `envelope` immediately cuts to 2048; a zoomed chart keeping a selected frame outside its window, plus the scene cache being keyed per panel rather than per window; unguarded non-finite overlay values; `has_data()` ignoring `historic` so an overlay-only chart would drop silently; and the ignore-chip editor as the reports page's one remaining gap vs ggo-ide) — and two stale claims elsewhere in `MIGRATION.md` were corrected rather than left to rot (the "no historic overlay yet" line under the Emulate-this-world entry, and the hover-rebuild ceiling's pre-memo shape).
- **2026-08-10 F5.4 process note (kept here because this log is not rewritten after the fact).** Five of the six F5.4 task reports contained at least one figure quoted from an **earlier state of the tree** than the one shipped — a test count, a query count, a line count carried forward from a prior round instead of re-measured. Two tasks shipped a **test written over a fixture that could not exercise the mechanism it named**: R5's drag-band test and R5's age-ramp test both passed against data that made the assertion vacuous. Neither class was caught by the suite, by clippy, or by self-review; both were caught only by **adversarial review with re-measurement** — a reviewer who re-ran the number rather than reading the sentence, and who substituted a paraphrase into an empty-state denylist to show it let the paraphrase through. (That denylist is now a character-for-character pin, for the same reason.) The lesson is procedural and it is about this phase's *method*, not its people: a figure in a report is a claim, and a claim that nobody re-ran is not evidence. Later phases should assume the same failure rate and budget for the re-measurement pass rather than treating it as an exception.
- 2026-08-10 merge drill #10 (F5.4 wrap, real): `git rev-list --count ggo..upstream/main` = 1 (`6634c945d3` "git_panel: Add copy path actions to the context menu (#62352)" — 5 files: the three `assets/keymaps/default-*.json`, `crates/git_ui/src/git_panel.rs`, `crates/project/src/git_store.rs`); `git checkout main && git pull upstream main && git push <https origin> main` fast-forward `a1860ac1c1..6634c945d3`; `git checkout ggo && git merge upstream/main --no-edit` clean as `db009aecb5`, 0 conflicts, no GGO-marked file touched. **Keymap check, new to this drill because the upstream commit adds bindings**: it claims `ctrl-alt-c` / `alt-ctrl-shift-c` for `workspace::CopyPath` / `CopyRelativePath`; the fork's `ctrl-alt-` bindings are `ctrl-alt-r` (Run), `ctrl-alt-s` (Stop) and `ctrl-alt-m` (Toggle Mute, new in F5.4/R6) — no collision. Marker counts unchanged from drill #9 (root `Cargo.toml` 16, `crates/zed/Cargo.toml` 9, `crates/zed/src/main.rs` 9, `dock.rs` 4, `workspace.rs` 11, `project_panel.rs` 9). `reload_keymaps` still ends with `keymap_editor::KeymapEventChannel::trigger_keymap_changed` (`crates/zed/src/zed.rs:2401`). Full ten-crate + workspace/project_panel/clippy/fmt/licenses/`cargo check -p zed` sweep re-run clean post-merge — same 703/237/120 with no deltas from the merge (see the F5.4 wrap line above, run after this drill).
